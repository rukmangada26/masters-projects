# Public notebooks (`*_public.ipynb`)


- `Predictive_Proj_Code_public.ipynb` uses `filtered_manufacturers_data.csv` from this folder (or `Data/` / `data/`).
- `Predictive_proj_public.ipynb` uses this folder as `PROJECT_DIR` and falls back to `Data/` if that subfolder exists.

Both notebooks are scrubbed (placeholder labels like `MFG_ALPHA`) and have cleared outputs.
